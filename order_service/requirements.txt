fastapi==0.111.0
uvicorn[standard]==0.29.0
django==5.0.4
psycopg2-binary==2.9.9
python-dotenv==1.0.1
python-multipart==0.0.9

# HTTP client — for calling Product and Payment services
httpx==0.27.0

# Redis client — for publishing events to message broker
redis==5.0.4

# JWT handling — required for authentication
python-jose[cryptography]==3.3.0
